# 로컬 개발·재현 파이프라인용. Vercel 함수 번들에는 설치되지 않는다.
-r requirements.txt
pandas>=2.0        # 마스터 엑셀 → 데이터 계약 로딩 (kets/config.py)
openpyxl>=3.1      # .xlsx 리더
matplotlib>=3.7    # scripts/build_figures.py
pytest>=7.4
pytest-cov>=4.1
